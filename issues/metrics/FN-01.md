# FN-01 verification

Verdict: pass. Score 93, no vetoes, one round.

## Summary
`src/dom.js` implements the readiness wait with timeout, the display lookup, the injection runner isolated by try/catch and the observer that cleans up before re-running. `src/index.js` orchestrates it and `theme.js` is regenerated and byte-identical to the build. Lint, typecheck, 23 tests and build are green. The mount tests catch real mutations (removing the cleanup call fails 2, rethrowing in the runner fails 1, dropping the timeout fails 1). Left open: the remount was evidenced by a DOM swap through the debug port, not by a real device switch.

## Acceptance criteria
- [x] `pnpm build` produces `theme.js` as an IIFE with no `import` or `export`. `theme.js:2` opens the IIFE, no module syntax by regex, `node --check` passes, build output identical to the committed file.
- [x] Vitest test for the build. `test/build.test.js:38-54`, reused from FN-00.
- [x] Vitest test for `mount` with jsdom. `test/dom.test.js:1` docblock, `:83-93` run that throws, `:115-125` cleanup before re-run.
- [x] In Spotify, the console shows `mounted` and re-injection works. Not reproduced by the verifier; evidence recorded in the issue (console via CDP, probe injection run / cleanup / run after the widget swap).
- [x] `pnpm check` green. Lint, typecheck, 23 tests, build.

## Findings
- scope, low, `src/dom.js:119`: real device switch not exercised; the aside-replaced case stays an open unknown, mitigation already in the plan (observe body always).
- discipline, low, `issues/_index.md:23`: status flipped to done at verification.
- reuse, low, `test/dom.test.js:119`: widget creation repeated three times in the test file.
- architecture, low, `src/dom.js:121`: no distinct warning for the body fallback.
- guidelines, low, `src/dom.js:91`: cleanup log format adds the phase; no change needed.

## Cost and time
Implementation about $5.99, verification about $1.34 (main session only; the verifier subagent's usage is not in the log). 46 active minutes over 326 wall-clock minutes, 9 user messages, 42 tool calls, one model (claude-opus-5). Attribution approximate: the session log carries `gitBranch=HEAD`, so the slice is the transcript from the FN-07 merge onward.
