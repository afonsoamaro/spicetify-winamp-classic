# FN-03 verification

Verdict: pass. Score 88, no vetoes, one round.

## Summary
The analyser is delivered as planned: pure state in `src/spectrum.js` with faithful tests, canvas drawing and a self-stopping rAF loop in `src/spectrum-dom.js` with jsdom tests over a recorded 2D context and a fake scheduler, `theme.js` regenerated byte-identical to a fresh build, all four checks green. The gaps are an untested resize guard, duplicated test helpers, and an out-of-plan (but justified and tested) marquee perf fix. The CPU criterion is honest about depending on FN-09 for long titles; the residual cost is pre-existing from FN-02, so it is not missing scope.

## Acceptance criteria
- [x] Tests for `nextFrame`. `test/spectrum.test.js:31-72`.
- [x] Tests for `colorForRow`. `test/spectrum.test.js:100-117`, bands and clamps.
- [x] In Spotify, bars move while playing and stop after a pause; paused CPU matches the no-extension baseline. Not reproduced by the verifier; issue record: `toDataURL` differs while playing, rAF 0/s paused, 98.7% idle with a static marquee; long titles depend on FN-09.
- [x] `pnpm build` run, `theme.js` updated, `pnpm check` green. Fresh build identical (632 lines), 85 tests.

## Findings applied after the verdict
`aria-hidden` on the canvas, `position: relative` on the display rule and a `[hidden]` rule for the canvas, the CPU criterion annotated with the FN-09 caveat, the description corrected to 19 bars, the stale comment in `user.css` reworded, and the marquee text-node fix committed on its own. Gate re-run green.

## Findings left open
- ResizeObserver guard untested (jsdom lacks the API).
- `fakePlayer`/`makeDisplay` duplicated between the two DOM test files; the guard and safe-remove blocks duplicated between the two injections. Worth extracting when FN-05 adds the third.
- No `prefers-reduced-motion` handling; candidate for a small issue.
- Hex fallbacks in `COLOR_TOKENS` mirror `color.ini`.

## Cost and time
Implementation about $32.04, verification about $3.06 (main session only; the verifier's usage is not in the log). 89 active minutes over 769 wall-clock minutes, 11 user messages, 70 tool calls, one model (claude-opus-5). Attribution approximate: the slice also carries the FN-08 diagnosis, which happened on this branch's timeline.
